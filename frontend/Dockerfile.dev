# DEVELOPMENT Dockerfile for React Frontend
# Uses Node development server with hot reloading
# For production, use Dockerfile instead

FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (including devDependencies)
RUN npm install

# Copy source code
COPY . .

# Expose development server port
EXPOSE 3000

# Start development server with hot reloading
# Note: Code changes are detected via volumes mounted in docker-compose.yml
CMD ["npm", "start"]
